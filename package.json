{
  "name": "epoxy",
  "version": "1.0.0",
  "description": "Epoxy is a reverse proxy, that should take off the load from any given mobile client.",
  "main": "server.js",
  "scripts": {
    //"test": "mocha"
    "test": "NODE_ENV=test YOURPACKAGE_COVERAGE=1 ./node_modules/.bin/mocha --require blanket --reporter mocha-lcov-reporter | ./node_modules/coveralls/bin/coveralls.js"
  },
  "author": "Oron Ben Zvi",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.12.4",
    "express": "^4.12.4",
    "morgan": "^1.5.3",
    "request": "^2.57.0",
    "winston": "^1.0.0",
    "xml2js": "^0.4.9"
  },
  "devDependencies": {
    "assert": "^1.3.0",
    "blanket": "^1.1.7",
    "coveralls": "^2.11.2",
    "jscoverage": "^0.5.9",
    "mocha": "^2.2.5"
  },
  "config": {
    "blanket": {
      "pattern": "app/**/*.js"
    }
  }
}
